name: tvshow_nav
description: 电视直播导航

publish_to: 'none'

version: 0.3.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  fluent_ui: ^4.13.0

  sqflite: ^2.3.3+1
  sqflite_common_ffi: ^2.3.5
  path_provider: ^2.1.4
  path: ^1.9.0

  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  generate: true
